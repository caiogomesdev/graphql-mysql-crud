# import Usuario from 'Usuario.graphql'

type Mutation{
    createUser(nome: String!, email: String!): Usuario
    updateUser(id: Int!, data: UserInput): Usuario
    deleteUser(id: Int!): Usuario
}

input UserInput{
    nome: String,
    Email: String
}